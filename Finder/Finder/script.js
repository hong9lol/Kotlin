var bodyText = null;
function init() {
chrome.tabs.executeScript({
    code:'document.querySelector("body").innerHTML;'
}, function(result){

        var user = this['kotlin-js'].domain.User.prototype.study2();
});

}